# Runtime dependencies — MetodoBase Web (server only, no desktop GUI)
# Use this for Railway/Docker/cloud deployments.
# CACHE INVALIDATION: 2026-04-02-v8-PERF100
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.0
pydantic>=2.0.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
reportlab>=4.0.0,<5
passlib[bcrypt]>=1.7.4,<2
bcrypt>=4.0.0,<5
sqlalchemy>=2.0.0,<3
alembic>=1.13.0,<2
psycopg2-binary>=2.9.0,<3  # PostgreSQL adapter (Railway DATABASE_URL compatible)
stripe>=11.0.0,<12
mercadopago>=2.2.0  # MercadoPago Python SDK
resend>=2.0.0,<3
requests>=2.31.0
httpx>=0.27.0,<1
cryptography>=41.0.0
sentry-sdk[fastapi]>=2.0.0
redis[hiredis]>=5.0.0,<6  # Distributed rate limiting + caching (requires REDIS_URL env var)
